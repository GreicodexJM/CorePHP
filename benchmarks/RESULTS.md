# Benchmark Results

> Auto-generated by `run.sh`. Do not edit by hand.

Persistent (**CorePHP** / RoadRunner) vs cold-start-per-request (**PHP-FPM** + nginx, opcache on),
both serving the **identical** application. The app has a framework-style `bench_bootstrap()`
phase (config tree, route compilation, service container): CorePHP runs it **once per worker**,
PHP-FPM runs it **on every request** (its process dies each time). Higher req/s and lower latency are better.

| Runtime | Requests/sec | Latency p50 | p90 | p99 |
|---|--:|--:|--:|--:|
| **CorePHP** (persistent) | 11918.18 | 3.82ms | 5.24ms | 6.95ms |
| PHP-FPM (cold start) | 1915.65 | 24.06ms | 30.61ms | 43.63ms |

**CorePHP throughput: 6.2× PHP-FPM.**

<sub>Config: 4 threads, 50 connections, 12s (after 3s warmup).
Both runtimes verified to return byte-identical responses. Single-machine, synthetic workload —
reproduce with `./run.sh`; absolute numbers vary by hardware, the ratio is the point.</sub>
